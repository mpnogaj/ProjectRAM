<UserControl x:Class="RAMEditor.CustomControls.Host"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RAMEditor.CustomControls"
             xmlns:properties="clr-namespace:RAMEditor.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="150"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="8*" MinWidth="400"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" MaxHeight="50"/>
                <RowDefinition Height="9*"/>
                <RowDefinition Height="50" MinHeight="50"/>
            </Grid.RowDefinitions>
            <TextBlock 
                Grid.Row="0" 
                Text="{StaticResource memory}" 
                HorizontalAlignment="Center"
                VerticalAlignment="Center" 
                FontFamily="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontFamily}"
                FontSize="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontSize}" 
                FontWeight="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontWeight}"
                Foreground="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontColor}"/>
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <Grid>
                    <Grid.Resources>
                        <Style TargetType="Border">
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="Black"/>
                            <Style.Triggers>
                                <Trigger Property="Grid.Column" Value="0">
                                    <Setter Property="Background" Value="{Binding Source={x:Static properties:Settings.Default}, Path=AddressBack}"/>
                                </Trigger>

                                <Trigger Property="Grid.Column" Value="1">
                                    <Setter Property="Background" Value="{Binding Source={x:Static properties:Settings.Default}, Path=ValueBack}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0, 0, 7, 0"/>
                            <Style.Triggers>
                                <Trigger Property="Grid.Column" Value="0">
                                    <Setter Property="FontSize" Value="14"/>
                                </Trigger>

                                <Trigger Property="Grid.Column" Value="1">
                                    <Setter Property="FontSize" Value="14"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0">
                        <TextBlock Text="{StaticResource address}" FontFamily="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=AddressFontFamily}"
                                   FontSize="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=AddressFontSize}"
                                   FontWeight="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=AddressFontWeight}"
                                   Foreground="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=AddressFontColor}"/>
                    </Border>
                    <Border Grid.Column="1">
                        <TextBlock Text="{StaticResource value}" FontFamily="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=ValueFontFamily}"
                                   FontSize="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=ValueFontSize}"
                                   FontWeight="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=ValueFontWeight}"
                                   Foreground="{Binding 
                            Source={x:Static properties:Settings.Default}, 
                            Path=ValueFontColor}"/>
                    </Border>
                </Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="Memory"  Orientation="Vertical"/>
                </ScrollViewer>
            </StackPanel>
        </Grid>
        <GridSplitter ResizeDirection="Columns" Grid.Column="1" Height="Auto" Width="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"/>
        <Grid x:Name="LeftColumn" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="1.5*" MinHeight="100"/>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="1.5*" MinHeight="100"/>
                <RowDefinition Height="auto" MaxHeight="400"/>
            </Grid.RowDefinitions>
            <!--Input tape-->
            <StackPanel Grid.Row="0" Orientation="Vertical" VerticalAlignment="Bottom" Margin="0, 0, 0, 15">
                <TextBlock 
                    Margin="0, 10, 0, 0" 
                    HorizontalAlignment="Center" 
                    Text="{StaticResource inputTape}" 
                    FontFamily="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontFamily}"
                    FontSize="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontSize}" 
                    FontWeight="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontWeight}"
                    Foreground="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontColor}"/>
                <TextBox 
                    x:Name="InputTape" 
                    Margin="0, 10, 0, 0" 
                    HorizontalScrollBarVisibility="Visible"
                    FontFamily="{Binding Source={x:Static properties:Settings.Default}, Path=ITFontFamily}"
                    FontSize="{Binding Source={x:Static properties:Settings.Default}, Path=ITFontSize}" 
                    FontWeight="{Binding Source={x:Static properties:Settings.Default}, Path=ITFontWeight}"
                    Foreground="{Binding Source={x:Static properties:Settings.Default}, Path=ITFontColor}"
                    Background="{Binding Source={x:Static properties:Settings.Default}, Path=ITBackground}"
                    LostFocus="InputTape_LostFocus"/>
            </StackPanel>
            <!--Code-->
            <DockPanel Grid.Row="1" LastChildFill="True">
                <local:SimpleEditor x:Name="SimpleEditor" Visibility="Visible"/>
                <TextBox
                    x:Name="Code" 
                    Background="{Binding Source={x:Static properties:Settings.Default}, Path=TBBackground}"
                    FontFamily="{Binding Source={x:Static properties:Settings.Default}, Path=TBFontFamily}"
                    FontWeight="{Binding Source={x:Static properties:Settings.Default}, Path=TBFontWeight}"
                    FontSize="{Binding Source={x:Static properties:Settings.Default}, Path=TBFontSize}" 
                    Foreground="{Binding Source={x:Static properties:Settings.Default}, Path=TBFontColor}"
                    AcceptsReturn="True" 
                    AcceptsTab="True" 
                    DockPanel.Dock="Top"
                    Visibility="Collapsed"
                    VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                    KeyDown="Code_KeyDown"/>
            </DockPanel>
            
            <!--Output tape-->
            <StackPanel Grid.Row="2" Orientation="Vertical" VerticalAlignment="Top" Margin="0, 15, 0, 0">
                <TextBox 
                    x:Name="OutputTape" 
                    Margin="0, 0, 0, 10" 
                    IsReadOnly="True" 
                    HorizontalScrollBarVisibility="Visible"
                    FontFamily="{Binding Source={x:Static properties:Settings.Default}, Path=OTFontFamily}"
                    FontSize="{Binding Source={x:Static properties:Settings.Default}, Path=OTFontSize}" 
                    FontWeight="{Binding Source={x:Static properties:Settings.Default}, Path=OTFontWeight}"
                    Foreground="{Binding Source={x:Static properties:Settings.Default}, Path=OTFontColor}"
                    Background="{Binding Source={x:Static properties:Settings.Default}, Path=OTBackground}"/>
                <TextBlock 
                    Margin="0, 0, 0, 10" 
                    HorizontalAlignment="Center" 
                    Text="{StaticResource outputTape}" 
                    FontFamily="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontFamily}"
                    FontSize="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontSize}" 
                    FontWeight="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontWeight}"
                    Foreground="{Binding Source={x:Static properties:Settings.Default}, Path=HDFontColor}"/>
            </StackPanel>

            <local:BottomDock x:Name="BottomDock" Grid.Row="3" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</UserControl>
